{
  "name": "thumbgo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "tester-app",
    "web"
  ],
  "scripts": {
    "dev:tester": "npm run dev -w tester-app",
    "build:tester": "npm run build -w tester-app",
    "lint:tester": "npm run lint -w tester-app",
    "test:tester": "npm run test -w tester-app"
  }
}
